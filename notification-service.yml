server:
  port: 8086

spring:
  application:
    name: NOTIFICATION-SERVICE

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  # 🔥 TEMP COMMENT EMAIL (FIRST RUN)
  # mail:
  #   host: smtp.gmail.com
  #   port: 587
  #   username: thakurritika361@gmail.com
  #   password: uhvqpnzisghopqvd
  #   properties:
  #     mail.smtp.auth: true
  #     mail.smtp.starttls.enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

management:
  endpoints:
    web:
      exposure:
        include: "*"
